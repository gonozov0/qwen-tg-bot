[project]
name = "qwen-tg-bot"
version = "0.1.0"
description = "Telegram bot proxying messages to qwen CLI"
requires-python = ">=3.9"
dependencies = [
    "aiogram>=3.0,<4.0",
    "python-dotenv>=1.0",
]
